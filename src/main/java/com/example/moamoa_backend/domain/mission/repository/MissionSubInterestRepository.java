package com.example.moamoa_backend.domain.mission.repository;

import com.example.moamoa_backend.domain.mission.entity.mapping.MissionSubInterest;

import org.springframework.data.jpa.repository.JpaRepository;

public interface MissionSubInterestRepository extends JpaRepository<MissionSubInterest, Long> {
}
